# Use the official Bun image
FROM oven/bun:latest

# Set working directory
WORKDIR /ORMI-CORE


# Copy all folders from the host to the container
COPY . .

# Run the install script
RUN ./build.sh



CMD cd ormi-app && bun run start