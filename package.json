{
  "name": "ormi-monorepo",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules package-lock.json bun.lock* .next && rm -rf ormi-*/node_modules ormi-*/dist ormi-*/bun.lock*",
    "build": "bun run clean && bash build.sh",
    "quick-build": "bash build.sh",
    "build:win": "build.bat",
    "dev": "bun --filter ormi-app run dev"
  },
  "workspaces": [
    "ormi-*"
  ]
}
